 It doesent cause re-render when state update.
 it's mainly used  for:
 Accessing DOM element.
 Storing mutable values without triggering re-rendores.
 Keeping reference across renders (e.g timers, previous value).